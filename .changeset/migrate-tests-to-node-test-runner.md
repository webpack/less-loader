---
"less-loader": major
---

Migrate the test suite to Node.js's built-in test runner (`node:test` / `node:assert`), drop Jest and `babel-jest`, and bump the minimum supported Node.js version to `>= 22.11.0`.
